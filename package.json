{
  "name": "schedule-manager-worker",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "CLOUDFLARE_ENV=dev vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit --strict",
    "preview": "npm run build && vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "npx wrangler types",
    "migrate:test:local": "npx wrangler d1 migrations apply test-schedule-manager --local",
    "migrate:test:remote": "npx wrangler d1 migrations apply test-schedule-manager --remote",
    "migrate:dev:local": "npx wrangler d1 migrations apply dev-schedule-manager --local",
    "migrate:dev:remote": "npx wrangler d1 migrations apply dev-schedule-manager --remote",
    "migrate:prod:local": "npx wrangler d1 migrations apply prod-schedule-manager --local",
    "migrate:prod:remote": "npx wrangler d1 migrations apply prod-schedule-manager --remote",
    "migrate:all:local": "npm run migrate:test:local && npm run migrate:dev:local && npm run migrate:prod:local",
    "migrate:all:remote": "npm run migrate:test:remote && npm run migrate:dev:remote && npm run migrate:prod:remote",
    "test:server": "CLOUDFLARE_ENV=test vite --port 5173",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "@types/date-fns": "^2.5.3",
    "date-fns": "^4.1.0",
    "hono": "^3.11.7",
    "hono-simple-google-auth": "^0.5.0",
    "ical-generator": "^9.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.4",
    "@cloudflare/workers-types": "^4.20250522.0",
    "@playwright/test": "^1.52.0",
    "@solidjs/router": "^0.15.3",
    "@solidjs/start": "^1.1.4",
    "@tailwindcss/vite": "^4.1.7",
    "@types/node": "^22.15.2",
    "autoprefixer": "^10.4.21",
    "daisyui": "^5.0.37",
    "dotenv-cli": "^8.0.0",
    "playwright": "^1.52.0",
    "postcss": "^8.5.3",
    "postcss-nesting": "^13.0.1",
    "solid-js": "^1.9.7",
    "tailwindcss": "^4.1.7",
    "typescript": "^5.4.5",
    "vite": "^6.3.5",
    "vite-plugin-solid": "^2.11.6",
    "wrangler": "^4.16.1"
  }
}
